# This "input" configures a global authorization rule to enable public access to
# all models in this schema. Learn more about authorization rules here: https://docs.amplify.aws/cli/graphql/authorization-rules
# input AMPLIFY { globalAuthRule: AuthRule = { allow: public } } # FOR TESTING ONLY!



type Location @model @auth(rules: [{allow: owner, operations: [create, update, delete]}]){
    user_id: String!
    time: AWSDateTime!
    latitude: Float!
    longitude: Float!
}

type User @model @auth(rules: [{allow: owner, operations: [create, update, delete]}]){
    id: ID!
    email_address: AWSEmail!
    username: String!
    password: String!
}

type Employee @model @auth(rules: [{allow: owner, operations: [create, update, delete]}]){
    user: User!
    current_location: Location!
}

type Customer @model @auth(rules: [{allow: owner, operations: [create, update, delete]}]){
    user: User!
    current_location: Location!
}


